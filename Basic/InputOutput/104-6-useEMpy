# **를 입력하면 사전을 입력받은 것으로 판단하고 인자를 하나만 받게된다.
band = 'nirvana'
name = 'cobain'

print('{name!s} is {band!s}'.format(**locals())) # str()와 결과가 동일
print('{name!r} is {band!r}'.format(**locals())) # repr()와 결과가 동일
print('{name!s} is {band!s}'.format(**locals())) # ascii()와 결과가 동일