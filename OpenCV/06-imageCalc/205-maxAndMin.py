import cv2 as cv

import numpy as np

# 최대ㅅ값 함수
# dst = cv.max(src1, src2)

# 최소ㅅ값 함수
# dst = cv.min(src1, src2)

# 최소/최대 값,위치 반환 함수
# minVal, maxVal, minLoc, maxLoc = np.minMaxLoc() # 책 오타...
# minVal, maxVal, minLoc, maxLoc = cv.minMaxLoc(src))